# Copyright Advanced Micro Devices, Inc.
# SPDX-License-Identifier: MIT

name: Test Native Linux Packages Install

on:
  workflow_call:
    inputs:
      package_install_url:
        description: "Package installation URL (includes architecture subdirectory for RPM)."
        required: true
        type: string
      release_type:
        description: "Release type for the packages being tested."
        required: true
        type: string
      gpg_key_url:
        description: "GPG key URL for signed repos"
        required: false
        type: string
        default: ""
      os_profile:
        description: OS profile (e.g., ubuntu2404, rhel8, rhel10, sles16). Package type (deb/rpm) is derived from this.
        required: true
        type: string
      artifact_group:
        description: Artifact group to extract GPU architecture from (e.g., gfx94X-dcgpu). Default gfx94X-dcgpu.
        required: false
        type: string
        default: "gfx94X-dcgpu"
      install_prefix:
        description: Installation prefix path (optional, defaults to /opt/rocm/core).
        required: false
        type: string
        default: "/opt/rocm/core"
      test_type:
        description: "Test type: quick, standard, comprehensive, full, install, or sanity. CI test types are mapped to native install test modes."
        required: false
        type: string
        default: ""
      repository:
        description: "Repository to checkout. Otherwise, defaults to `github.repository`"
        type: string
        required: false
      ref:
        description: "Branch, tag or SHA to checkout. Defaults to the reference or SHA that triggered the workflow"
        type: string
        required: false
      test_runs_on:
        description: >-
          GitHub runner label for the test job (Linux only). The runner should
          match the GPU pool for artifact_group (e.g.
          linux-gfx942-1gpu-ccs-csp-ossci-rocm for gfx94X-dcgpu). Windows
          runner labels are rejected.
        required: false
        type: string
        default: "linux-gfx942-1gpu-ccs-csp-ossci-rocm"
      build_variant:
        description: "Build variant (e.g. 'asan', 'host-asan', 'asan-debug', 'host-asan-debug'). Changes expected package names to match variant-suffixed packages."
        required: false
        type: string
        default: ""
      quartz_tracking_id:
        description: >-
          Leave empty. Internal Quartz tracking id, set automatically by the
          top-level orchestrator to link this run to its release lineage. Do not
          set it manually.
        type: string
        default: ""
  workflow_dispatch:
    inputs:
      package_install_url:
        description: "Package installation URL (includes architecture subdirectory for RPM)"
        required: true
        type: string
      release_type:
        description: "Release type for the packages being tested."
        required: true
        type: choice
        options:
          - ci
          - dev
      gpg_key_url:
        description: "GPG key URL for signed repos"
        type: string
        default: ""
      os_profile:
        description: OS profile (package type deb/rpm is derived from this).
        required: true
        type: choice
        options:
          - ubuntu2404
          - rhel8
          - rhel10
          - sles16
      artifact_group:
        description: Artifact group to extract GPU architecture from (e.g., gfx94X-dcgpu)
        type: string
        default: "gfx94X-dcgpu"
      install_prefix:
        description: Installation prefix path
        type: string
        default: "/opt/rocm/core"
        required: false
      test_type:
        description: "Test type: quick, standard, comprehensive, full, install, or sanity (optional; leave empty for sanity)"
        type: string
        default: ""
      repository:
        description: "Repository to checkout. Otherwise, defaults to `github.repository`"
        type: string
      ref:
        description: "Branch, tag or SHA to checkout. Defaults to the reference or SHA that triggered the workflow"
        type: string
      test_runs_on:
        description: >-
          GitHub runner label for the test job (Linux only). The runner should
          match the GPU pool for artifact_group (e.g.
          linux-gfx942-1gpu-ccs-csp-ossci-rocm for gfx94X-dcgpu). Windows
          runner labels are rejected.
        required: false
        type: string
        default: "linux-gfx942-1gpu-ccs-csp-ossci-rocm"
      build_variant:
        description: "Build variant (e.g. 'asan'). Changes expected package names."
        type: string
        default: ""
      quartz_tracking_id:
        description: >-
          Leave empty. Internal Quartz tracking id, set automatically by the
          top-level orchestrator to link this run to its release lineage. Do not
          set it manually.
        type: string
        default: ""

permissions:
  id-token: write
  contents: read

run-name: Test - Native Linux packages Install (${{ inputs.os_profile }})

jobs:
  prepare_install_context:
    name: Prepare install context (package type, GPU arch, container image, container_gpu_options)
    runs-on: ubuntu-24.04
    outputs:
      pkg_type: ${{ steps.derive.outputs.pkg_type }}
      gfx_arch: ${{ steps.derive.outputs.gfx_arch }}
      container_image: ${{ steps.derive.outputs.container_image }}
      # Emitting container_gpu_options from this job keeps OS-specific differences in one
      # place and lets the install matrix grow without duplicating container blocks.
      container_gpu_options: ${{ steps.derive.outputs.container_gpu_options }}
    steps:
      - name: Validate test_runs_on input
        if: ${{ github.repository_owner == 'ROCm' }}
        run: |
          RUNNER="${{ inputs.test_runs_on || 'linux-gfx942-1gpu-ccs-csp-ossci-rocm' }}"
          if echo "${RUNNER}" | grep -qi 'windows'; then
            echo "::error::test_runs_on must be a Linux runner label (got: ${RUNNER})"
            exit 1
          fi
          echo "Using runner label: ${RUNNER}"

      - name: "Quartz - started - test rpm/deb install"
        continue-on-error: true
        if: ${{ vars.NOTIFY_QUARTZ_ENABLED == 'true' }}
        uses: ROCm/Quartz/.github/actions/notify_quartz@f386a9756620938616af0b4d5d04b24ae6e0353f # notify_quartz/v1.1.1
        with:
          run_phase: started
          reporting_workflow: test_native_linux_packages_install.yml
          workflow_inputs: ${{ toJSON(inputs) }}
          gh_app_client_id: ${{ secrets.GH_APP_HAULY_CID }}
          gh_app_private_key: ${{ secrets.GH_APP_HAULY_PRIVATE_KEY }}

      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          repository: ${{ inputs.repository || github.repository }}
          ref: ${{ inputs.ref || '' }}

      - name: Derive package type, GPU architecture, and container image
        id: derive
        run: |
          case "${{ inputs.os_profile }}" in
            ubuntu*|debian*) PKG_TYPE="deb" ;;
            *) PKG_TYPE="rpm" ;;
          esac
          echo "pkg_type=${PKG_TYPE}" >> $GITHUB_OUTPUT
          python build_tools/packaging/linux/get_url_repo_params.py extract-gfx-arch \
            --artifact-group "${{ inputs.artifact_group }}"
          python build_tools/packaging/linux/get_url_repo_params.py get-container-image \
            --os-profile "${{ inputs.os_profile }}"

          # Using Minimal GPU passthrough for native install basic verification (rocminfo).
          # Device nodes are required so rocminfo can reach the AMDGPU driver on
          # OSSCI GPU runners.
          #
          # Options are emitted as this job output (not inlined on the test job) so
          # we can vary them per os_profile. Install CI uses different base images
          # (Ubuntu, RHEL/UBI, SLES BCI), Extend based on specific os_profile needs.
          CONTAINER_GPU_OPTIONS="--device /dev/kfd --device /dev/dri"
          echo "container_gpu_options=${CONTAINER_GPU_OPTIONS}" >> $GITHUB_OUTPUT

  test_native_linux_packages_install:
    name: Test Native Linux Packages Installation - ${{ inputs.os_profile }}
    needs: [prepare_install_context]
    strategy:
      fail-fast: false
    runs-on: ${{ github.repository_owner == 'ROCm' && (inputs.test_runs_on || 'linux-gfx942-1gpu-ccs-csp-ossci-rocm') || 'ubuntu-24.04' }}
    container:
      image: ${{ needs.prepare_install_context.outputs.container_image }}
      # GPU options from prepare_install_context — see comment there for why this
      # is dynamic across os_profile (multi-OS install matrix, SLES /etc/group).
      options: ${{ needs.prepare_install_context.outputs.container_gpu_options }}
    env:
      REPO_URL: ${{ inputs.package_install_url }}
      GPG_KEY_URL: ${{ inputs.gpg_key_url || '' }}
      OS_PROFILE: ${{ inputs.os_profile }}
      PKG_TYPE: ${{ needs.prepare_install_context.outputs.pkg_type }}
      RELEASE_TYPE: ${{ inputs.release_type }}
      GFX_ARCH: ${{ needs.prepare_install_context.outputs.gfx_arch }}
      INSTALL_PREFIX: ${{ inputs.install_prefix || '/opt/rocm/core' }}
      TEST_TYPE: ${{ inputs.test_type }}
      BUILD_VARIANT: ${{ inputs.build_variant }}
    steps:
      - name: Install System Prerequisites
        run: |
          echo "Installing prerequisites for ${{ env.PKG_TYPE }} on container"

          if [ "${{ env.PKG_TYPE }}" = "deb" ]; then
            sudo apt update
            sudo apt install -y \
              wget \
              curl \
              ca-certificates \
              gpg \
              lsb-release \
              binutils
            if [ "$TEST_TYPE" = "full" ] || [ "$TEST_TYPE" = "comprehensive" ]; then
              echo "Extra OS packages for --test-type full/comprehensive (e.g. rdhc): pciutils, kmod, apt-utils"
              sudo apt install -y pciutils kmod apt-utils
            fi
          elif [[ "${{ inputs.os_profile }}" == sles* ]]; then
            # SLES prerequisites (uses BCI base - zypper)
            zypper refresh
            zypper install -y \
              wget \
              curl \
              ca-certificates \
              gpg2 \
              lsb-release \
              update-alternatives \
              binutils
            if [ "$TEST_TYPE" = "full" ] || [ "$TEST_TYPE" = "comprehensive" ]; then
              echo "Extra OS packages for --test-type full/comprehensive on SLES (e.g. rdhc): pciutils, kmod"
              zypper install -y pciutils kmod
            fi
          else
            # rpm (non-SLES on RHEL/UBI); --allowerasing for curl vs curl-minimal on UBI
            dnf install -y --allowerasing \
              wget \
              curl \
              ca-certificates \
              gnupg2 \
              dnf-plugins-core \
              binutils
            if [ "$TEST_TYPE" = "full" ] || [ "$TEST_TYPE" = "comprehensive" ]; then
              echo "Extra OS packages for --test-type full/comprehensive (e.g. rdhc): pciutils, kmod"
              dnf install -y --allowerasing pciutils kmod
            fi
          fi

      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          repository: ${{ inputs.repository || github.repository }}
          ref: ${{ inputs.ref || '' }}

      - name: Setup Python and install runtime
        run: |
          bash build_tools/packaging/linux/setup_python_cmd.sh \
            --os-profile "${{ inputs.os_profile }}" \
            --install-runtime \
            --output-format github

      - name: Install Python Dependencies
        run: |
          # Use venv for all distros for consistency and future-proofing (PEP 668)
          $PYTHON_CMD -m venv /tmp/test-venv
          /tmp/test-venv/bin/python -m pip install --upgrade pip
          /tmp/test-venv/bin/python -m pip install -r build_tools/packaging/linux/tests/requirements.txt
          echo "PYTHON_CMD=/tmp/test-venv/bin/python" >> $GITHUB_ENV

      - name: Package Installation Test
        run: |
          echo "TEST - OS:${{ inputs.os_profile }} REPO:${{ env.REPO_URL }} Arch:${{ env.GFX_ARCH }} Type:${{ env.RELEASE_TYPE }}"
          $PYTHON_CMD -m pytest build_tools/packaging/linux/native_linux_package_install_test.py -vv --tb=long -s

  notify_quartz_completed:
    if: ${{ always() && vars.NOTIFY_QUARTZ_ENABLED == 'true' }}
    needs: [prepare_install_context, test_native_linux_packages_install]
    name: "Quartz - completed - test rpm/deb install"
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    continue-on-error: true
    steps:
      - name: "Quartz - completed - test rpm/deb install"
        uses: ROCm/Quartz/.github/actions/notify_quartz@f386a9756620938616af0b4d5d04b24ae6e0353f # notify_quartz/v1.1.1
        with:
          run_phase: completed
          reporting_workflow: test_native_linux_packages_install.yml
          workflow_inputs: ${{ toJSON(inputs) }}
          workflow_captured_outputs: ${{ toJSON(needs) }}
          gh_app_client_id: ${{ secrets.GH_APP_HAULY_CID }}
          gh_app_private_key: ${{ secrets.GH_APP_HAULY_PRIVATE_KEY }}
